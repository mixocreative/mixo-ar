{
  "name": "mixo-ar-standalone",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "test:browser": "node --test tests/browser/*.test.mjs",
    "test:all": "npm run test && npm run test:browser"
  },
  "devDependencies": {
    "playwright": "^1.63.0",
    "three": "^0.180.0"
  }
}
